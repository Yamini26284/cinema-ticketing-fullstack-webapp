# Copy to `.env.local` (or `.env`) and edit if needed.
# Assumes Postgres on localhost:5432 and Redis on localhost:6379.
DATABASE_URL=postgres://postgres@localhost:5432/pensive
REDIS_URL=redis://localhost:6379
PORT=3001
